<h5><%= gettext("You need to login before access the codecasts") %></h5>
<hr class='divider' />
<%= link gettext("Sign in with Google"), to: session_path(@conn, :request, :google), class: "btn btn-lg btn-block btn-primary" %>
